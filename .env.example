# Vismar Aqua - Environment Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# GENERAL SETTINGS
# =============================================================================
NODE_ENV=development
PROJECT_NAME=vismar-aqua

# =============================================================================
# MYSQL DATABASE CONFIGURATION
# =============================================================================
MYSQL_ROOT_PASSWORD=rootpassword_change_me
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=wordpress_password_change_me

# =============================================================================
# WORDPRESS CONFIGURATION
# =============================================================================
# WordPress Debug Mode (1 for development, 0 for production)
WORDPRESS_DEBUG=1

# WordPress URLs (update for production)
WP_HOME=http://localhost:8080
WP_SITEURL=http://localhost:8080

# WordPress Admin Credentials (set on first install)
WORDPRESS_ADMIN_EMAIL=admin@vismar-aqua.com
WORDPRESS_ADMIN_USER=admin
WORDPRESS_ADMIN_PASSWORD=change_me_admin_password

# =============================================================================
# WORDPRESS API ENDPOINTS
# =============================================================================
# Internal Docker network URLs (for backend communication)
WORDPRESS_API_URL=http://wordpress
WORDPRESS_GRAPHQL_URL=http://wordpress/graphql
WORDPRESS_REST_API_URL=http://wordpress/wp-json

# =============================================================================
# NEXT.JS FRONTEND CONFIGURATION
# =============================================================================
# NOTE: Frontend service is currently disabled in docker-compose.yml
# These variables will be used when frontend is enabled in the next phase
# Public URLs (accessible from browser)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_WP_URL=http://localhost:8080
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:8080/graphql
NEXT_PUBLIC_REST_API_URL=http://localhost:8080/wp-json

# =============================================================================
# INTERNATIONALIZATION (i18n)
# =============================================================================
NEXT_PUBLIC_DEFAULT_LOCALE=ua
NEXT_PUBLIC_LOCALES=en,ua

# =============================================================================
# WORDPRESS PLUGINS API KEYS
# =============================================================================
# Add API keys for plugins as needed
# Example: ACF Pro License Key
ACF_PRO_LICENSE_KEY=

# Example: WPML License Key (if using WPML instead of Polylang)
WPML_LICENSE_KEY=

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# SMTP Settings for WordPress email (e.g., contact forms)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@vismar-aqua.com
SMTP_PASSWORD=
SMTP_FROM=noreply@vismar-aqua.com
SMTP_FROM_NAME=Vismar Aqua

# =============================================================================
# ANALYTICS & TRACKING
# =============================================================================
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_GTM_ID=

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================
# NextAuth (if using authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate_a_random_secret_key_here

# WordPress Application Passwords / JWT (for API authentication)
WP_APPLICATION_PASSWORD=

# =============================================================================
# RECAPTCHA (for contact forms)
# =============================================================================
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# =============================================================================
# IMAGE OPTIMIZATION
# =============================================================================
# Allowed image domains (comma-separated)
NEXT_IMAGE_DOMAINS=localhost,vismar-aqua.com

# =============================================================================
# REDIS CACHE (optional - for production)
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# =============================================================================
# PRODUCTION SETTINGS (uncomment and update for production)
# =============================================================================
# NODE_ENV=production
# WP_HOME=https://vismar-aqua.com
# WP_SITEURL=https://vismar-aqua.com
# NEXT_PUBLIC_SITE_URL=https://vismar-aqua.com
# NEXT_PUBLIC_WP_URL=https://vismar-aqua.com
# NEXT_PUBLIC_GRAPHQL_URL=https://vismar-aqua.com/graphql
# WORDPRESS_DEBUG=0

# =============================================================================
# SSL/TLS CONFIGURATION (production)
# =============================================================================
# SSL_CERT_PATH=/etc/letsencrypt/live/vismar-aqua.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/vismar-aqua.com/privkey.pem

# =============================================================================
# BACKUP CONFIGURATION
# =============================================================================
BACKUP_RETENTION_DAYS=30
BACKUP_PATH=/backups

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit .env file to version control (it's in .gitignore)
# 2. Generate strong passwords for production
# 3. Update all URLs when deploying to production
# 4. Enable SSL/HTTPS in production
# 5. Set WORDPRESS_DEBUG=0 in production
# 6. Use environment-specific .env files (.env.local, .env.production)
